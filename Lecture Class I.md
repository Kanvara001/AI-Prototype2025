# 💻 Command Line on Terminal


## 📂 1. List — การแสดงรายการไฟล์และโฟลเดอร์

คำสั่ง `ls` ใช้สำหรับแสดงไฟล์และโฟลเดอร์ที่อยู่ในตำแหน่งปัจจุบัน

```bash
ls
```

---

### 🔹 แสดงรายละเอียดไฟล์ (Long Format)

```bash
ls -l
```

ข้อมูลที่แสดงในรูปแบบ long format

```
Permission | จำนวนผู้ใช้งาน | ขนาดไฟล์ | วันและเวลา | ชื่อไฟล์
```

---

### 🔹 แสดงขนาดไฟล์แบบอ่านง่าย (Human Readable)

```bash
ls -lh
```

โครงสร้างข้อมูลที่ได้

```
Permission | Users | Size (Readable) | Date | File name
```

---

### 🔹 เรียงไฟล์ตามเวลาที่แก้ไขล่าสุด

```bash
ls -ltr
```

คำอธิบาย option

```
-t  → เรียงตามเวลา
-r  → กลับลำดับ
```

ไฟล์ที่ถูกแก้ไขล่าสุดจะแสดงอยู่ด้านล่างสุด

---

### 🔐 ความหมายของ Permission

```
r → read    (อ่านข้อมูลได้)
w → write   (เขียนหรือแก้ไขได้)
x → execute (รันไฟล์ได้)
```

---

## 📁 2. Change Directory — การเปลี่ยนตำแหน่งโฟลเดอร์

คำสั่ง `cd` ใช้สำหรับเปลี่ยนตำแหน่งโฟลเดอร์ที่กำลังทำงานอยู่

กลับไปยัง Home directory

```bash
cd
```

ย้อนกลับไปโฟลเดอร์ก่อนหน้า 1 ระดับ

```bash
cd ..
```

ย้อนกลับ 2 ระดับ

```bash
cd ../..
```

ย้ายไปยังโฟลเดอร์ที่ต้องการ

```bash
cd <folder_name>/
```

สามารถกด `Tab` เพื่อช่วยเติมชื่อโฟลเดอร์อัตโนมัติ

---

### 🔍 ตรวจสอบตำแหน่งปัจจุบัน

```bash
pwd
```

---

## 🗂️ 3. File & Folder Management

### 📁 สร้างโฟลเดอร์ใหม่

```bash
mkdir <folder_name>
```

---

### 📝 สร้างหรือแก้ไขไฟล์ด้วย vi

```bash
vi <file_name>
vi <file_name>.py
```

ขั้นตอนพื้นฐานในการใช้งาน vi

```
i     → เข้าโหมดเขียน (Insert Mode)
Esc   → ออกจากโหมดเขียน
:wq   → บันทึกและออกจากไฟล์
:q!   → ออกจากไฟล์โดยไม่บันทึก
```

---

### 🗑️ ลบไฟล์และโฟลเดอร์

ลบไฟล์

```bash
rm <file_name>
```

ลบโฟลเดอร์ทั้งหมด (รวมไฟล์ภายใน)

```bash
rm -r <folder_name>
```

---

### 📄 เปิดดูเนื้อหาไฟล์

```bash
cat <file_name>
```

---

## 🚚 4. Move File — การย้ายไฟล์

วิธีที่ 1 ระบุ path ต้นทางและปลายทาง

```bash
mv <source_path>/<file_name> <destination_path>/.
```

วิธีที่ 2 ย้ายไฟล์จากโฟลเดอร์ก่อนหน้า

```bash
mv ../<file_name> .
```

```
.  หมายถึงตำแหน่งปัจจุบัน
```

---

## 📑 5. Copy File / Folder

คัดลอกไฟล์

```bash
cp <source_path>/<file_name> <destination_path>/.
```

คัดลอกโฟลเดอร์ทั้งชุด

```bash
cp -r <folder_source> .
```

---

## 📖 6. Manual Page — คู่มือคำสั่ง

ใช้คำสั่ง `man` เพื่อดูรายละเอียดการใช้งานคำสั่งต่าง ๆ

```bash
man <command>
```

---

## 🖥️ 7. Check System Preference

ตรวจสอบการใช้ทรัพยากรของระบบ (CPU และ RAM)

```bash
htop
```

ต้องติดตั้งโปรแกรมก่อนใช้งาน

---

## 📝 Notes เพิ่มเติม

ตัวอย่าง Permission ของไฟล์

```
drwxr-xr-x
```

ความหมายของแต่ละตัวอักษร

```
d → directory
r → read
x → execute
```

สรุปคำสั่งที่ใช้บ่อย

```
ls -lh  → แสดงขนาดไฟล์แบบอ่านง่าย
ls -ltr → ไฟล์ที่แก้ไขล่าสุดอยู่ด้านล่างสุด
```

---
