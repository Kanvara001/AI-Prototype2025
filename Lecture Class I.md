# 💻 Command Line Essentials
> **📘 Lecture Class 1** | รวมคำสั่ง Terminal พื้นฐานที่ใช้บ่อย

---

### 📂 1. การดูรายชื่อไฟล์และโฟลเดอร์ (List)
<details>
  <summary>คลิกเพื่อดูคำสั่ง ls รูปแบบต่างๆ</summary>

  * `ls` : แสดงชื่อไฟล์และโฟลเดอร์ทั่วไป
  * `ls -l` : แสดงรายละเอียดแบบจัดเต็ม (สิทธิ์เข้าถึง, เจ้าของ, ขนาดไฟล์)
  * `ls -lh` : แสดงขนาดไฟล์ให้เราอ่านง่ายขึ้น (เช่น 10MB แทนการแสดงเป็น Byte)
  * `ls -ltr` : เรียงไฟล์ตามเวลาแก้ไข (อันใหม่สุดจะอยู่ล่างสุดเสมอ)
</details>

---

### 📁 2. การเดินทางในระบบ (Navigation)
<details>
  <summary>คลิกเพื่อดูวิธีเปลี่ยนตำแหน่งโฟลเดอร์</summary>

  * `pwd` : เช็กตำแหน่งปัจจุบันว่าเราอยู่ที่ไหน
  * `cd` : กลับไปยังโฟลเดอร์เริ่มต้น (Home)
  * `cd ..` : ย้อนออกไป 1 ระดับ
  * `cd ../..` : ย้อนออกไป 2 ระดับ
  * `cd <folder_name>/` : เข้าไปยังโฟลเดอร์ที่ต้องการ (กด `Tab` เพื่อเติมชื่ออัตโนมัติ)
</details>

---

### 🗂️ 3. การสร้างและจัดการไฟล์ (File Management)
<details>
  <summary>คลิกเพื่อดูวิธีสร้าง แก้ไข และลบไฟล์</summary>

  * `mkdir <name>` : สร้างโฟลเดอร์ใหม่
  * `cat <name>` : เปิดดูข้อความในไฟล์แบบรวดเร็ว
  * `vi <name>` : เปิดโปรแกรมแก้ไขไฟล์
      * **วิธีใช้ VI:** กด `i` เพื่อพิมพ์ > กด `Esc` > พิมพ์ `:wq` เพื่อเซฟและออก (หรือ `:q!` เพื่อทิ้ง)
  * `rm <name>` : ลบไฟล์
  * `rm -r <name>` : ลบโฟลเดอร์ (ต้องใส่ `-r` ทุกครั้ง)
</details>

---

### 🚚 4. การย้ายและคัดลอก (Move & Copy)
<details>
  <summary>คลิกเพื่อดูคำสั่งจัดการตำแหน่งไฟล์</summary>

  * **ย้ายไฟล์:** `mv <ต้นทาง> <ปลายทาง>`
  * **เปลี่ยนชื่อไฟล์:** `mv <ชื่อเดิม> <ชื่อใหม่>`
  * **คัดลอกไฟล์:** `cp <ต้นทาง> <ปลายทาง>`
  * **คัดลอกโฟลเดอร์:** `cp -r <ต้นทาง> <ปลายทาง>`
  * *สัญลักษณ์ `.` (จุด) หมายถึงตำแหน่งปัจจุบันที่เราอยู่*
</details>

---

### 🛠️ 5. เครื่องมือตรวจสอบระบบ (Utilities)
<details>
  <summary>คลิกเพื่อดูคำสั่งช่วยเหลืออื่นๆ</summary>

  * `man <command>` : ดูคู่มือการใช้คำสั่งนั้นๆ (Manual)
  * `htop` : ดูการทำงานของ CPU และ RAM แบบ Real-time
  * `history` : ย้อนดูคำสั่งทั้งหมดที่เคยพิมพ์ไป
</details>

---

### 🔐 สรุปการอ่านสิทธิ์ (Permissions)
<details>
  <summary>คลิกเพื่อดูวิธีอ่านค่า drwxr-xr-x</summary>

  เวลาใช้ `ls -l` จะเห็นสัญลักษณ์ในช่องแรก ให้จำง่ายๆ ดังนี้:
  * `d` : คือโฟลเดอร์ (Directory)
  * `r` : อ่านได้ (Read)
  * `w` : เขียน/แก้ไขได้ (Write)
  * `x` : รันโปรแกรมได้ (Execute)
</details>

---
