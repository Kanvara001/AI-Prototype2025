💻 Command Line Basics (Terminal)

📂 1. File Listing (การแสดงรายการไฟล์)

ใช้คำสั่ง ls เพื่อดูไฟล์และโฟลเดอร์ที่อยู่ใน directory ปัจจุบัน

คำสั่งพื้นฐาน
ls


แสดงรายชื่อไฟล์และโฟลเดอร์ทั้งหมด

ls -l


แสดงข้อมูลแบบละเอียด (Long Format)

Permission	Users	Size	Date	File name
ls -lh


แสดงขนาดไฟล์ในรูปแบบอ่านง่าย (KB, MB, GB)

ls -ltr


เรียงไฟล์ตามเวลาที่มีการแก้ไข

ไฟล์ล่าสุดจะอยู่ล่างสุด

-t = sort by time

-r = reverse order

ความหมายของ Permission

r → read (อ่าน)

w → write (เขียน/แก้ไข)

x → execute (รันไฟล์)

📁 2. Change Directory (เปลี่ยนโฟลเดอร์)

คำสั่ง cd ใช้สำหรับย้ายตำแหน่งการทำงานในระบบไฟล์

cd


กลับไปยัง Home Directory (~)

cd ..


ถอยกลับไปโฟลเดอร์ก่อนหน้า 1 ระดับ

cd ../..


ถอยกลับ 2 ระดับ

cd <folder_name>/


เข้าสู่โฟลเดอร์ที่ต้องการ
💡 สามารถกด Tab เพื่อช่วยเติมชื่อโฟลเดอร์อัตโนมัติ

pwd


แสดง path ของตำแหน่งปัจจุบัน

🗂️ 3. File & Folder Management
สร้างโฟลเดอร์
mkdir <folder_name>

สร้างหรือแก้ไขไฟล์ด้วย vi
vi <file_name>
vi <file_name>.py

การใช้งาน vi เบื้องต้น

i → เข้าสู่โหมดพิมพ์

Esc → ออกจากโหมดพิมพ์

:wq → บันทึกและออก

:q! → ออกโดยไม่บันทึก

ลบไฟล์
rm <file_name>

ลบโฟลเดอร์ (รวมไฟล์ทั้งหมด)
rm -r <folder_name>

เปิดดูเนื้อหาไฟล์
cat <file_name>

🚚 4. Move File (ย้ายไฟล์)
วิธีที่ 1 : ระบุ path ต้นทางและปลายทาง
mv <source_path>/<file_name> <destination_path>/.

วิธีที่ 2 : อยู่ในโฟลเดอร์ปลายทาง
mv ../<file_name> .


. หมายถึง directory ปัจจุบัน

📋 5. Copy File / Folder
Copy ไฟล์
cp <source_path>/<file_name> <destination_path>/.

Copy โฟลเดอร์
cp -r <folder_source> .

📖 6. Manual Page

ใช้ดูคู่มือและรายละเอียดของคำสั่งต่าง ๆ

man <command>

🖥️ 7. System Monitoring
htop


ใช้ตรวจสอบการใช้งาน CPU และ RAM แบบ real-time
⚠️ จำเป็นต้องติดตั้งก่อนใช้งาน

📝 Notes เพิ่มเติม

ตัวอย่าง permission:

drwxr-xr-x

Symbol	Meaning
d	directory
r	read
w	write
x	execute

คำสั่งที่ควรรู้:

ls -lh → แสดงขนาดไฟล์แบบอ่านง่าย

ls -ltr → ไฟล์ล่าสุดอยู่ด้านล่าง
