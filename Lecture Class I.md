# 💻 Command Line พื้นฐานบน Terminal
---

## 📂 1. การแสดงรายการไฟล์ (List Commands)
ใช้สำหรับตรวจสอบไฟล์และโฟลเดอร์ในตำแหน่งปัจจุบัน

| คำสั่ง | คำอธิบาย |
|:--- |:--- |
| `ls` | แสดงรายชื่อไฟล์/โฟลเดอร์ทั่วไป |
| `ls -l` | แสดงรายละเอียดแบบ **Long Format** (สิทธิ์, เจ้าของ, ขนาด, เวลา) |
| `ls -lh` | แสดงขนาดไฟล์แบบอ่านง่าย (**Human-readable** เช่น 1K, 234M, 2G) |
| `ls -ltr` | เรียงตามเวลาแก้ไขล่าสุด โดยให้ไฟล์ **ใหม่ล่าสุดอยู่ล่างสุด** |


---

## 📁 2. การย้ายตำแหน่ง (Navigation)
จัดการตำแหน่งที่เราทำงานอยู่ (Working Directory)

* `pwd` : **Print Working Directory** - แสดงตำแหน่งปัจจุบันที่เราอยู่
* `cd` : กลับไปยัง **Home directory** (`~`)
* `cd ..` : ย้อนกลับไป 1 ระดับ
* `cd ../..` : ย้อนกลับไป 2 ระดับ
* `cd <folder_name>/` : เข้าไปยังโฟลเดอร์ที่ระบุ (💡 *Tip: กด `Tab` เพื่อช่วยเติมชื่ออัตโนมัติ*)

---

## 🗂️ 3. การจัดการไฟล์และโฟลเดอร์ (Management)

### ✨ การสร้างและแก้ไข
* `mkdir <folder_name>` : สร้างโฟลเดอร์ใหม่
* `vi <file_name>` : สร้างหรือแก้ไขไฟล์ด้วย **VI Editor**

> **🛠️ คู่มือสำหรับ VI Editor:**
> 1.  **โหมดเขียน:** กด `i` (Insert)
> 2.  **โหมดคำสั่ง:** กด `Esc`
> 3.  **บันทึกและออก:** พิมพ์ `:wq` แล้ว Enter
> 4.  **ออกโดยไม่บันทึก:** พิมพ์ `:q!` แล้ว Enter

### 📝 การอ่านและการลบ
* `cat <file_name>` : แสดงเนื้อหาทั้งหมดภายในไฟล์
* `rm <file_name>` : ลบไฟล์ที่ระบุ
* `rm -r <folder_name>` : ลบโฟลเดอร์พร้อมข้อมูลภายในทั้งหมด (**ใช้อย่างระมัดระวัง**)

---

## 🚚 4. การเคลื่อนย้ายและคัดลอก (Move & Copy)

| การทำงาน | รูปแบบคำสั่ง |
|:--- |:--- |
| **ย้ายไฟล์ (แบบระบุ Path)** | `mv <source_path>/<file_name> <destination_path>/.` |
| **ดึงไฟล์มาที่ปัจจุบัน** | `mv ../<file_name> .` ( `.` หมายถึงโฟลเดอร์ปัจจุบัน) |
| **คัดลอกไฟล์** | `cp <source_path>/<file_name> <destination_path>/.` |
| **คัดลอกโฟลเดอร์** | `cp -r <folder_source> .` |

---

## 🛠️ 5. เครื่องมือช่วยเหลือและตรวจสอบระบบ

* `man <command>` : เปิดคู่มือ (Manual) เพื่อดูวิธีใช้คำสั่งต่างๆ อย่างละเอียด
* `htop` : ตรวจสอบการใช้งาน Resource ของเครื่อง (RAM, CPU) แบบ Real-time
    * *(ต้องติดตั้งเพิ่มเติมบนระบบบางตัว)*

---

## 💡 สรุปเกร็ดความรู้ (Quick Notes)

### 🔐 การอ่านค่า Permission
ตัวอย่าง: `drwxr-xr-x`
- `d` : คือ Directory (ถ้าเป็น `-` คือไฟล์ธรรมดา)
- `r` (Read) : อ่านไฟล์ได้
- `w` (Write) : แก้ไขไฟล์ได้
- `x` (Execute) : สั่งรันไฟล์ได้

### ⚡ คีย์ลัดจำง่าย
- `-t` : Time (เวลา)
- `-r` : Reverse (ย้อนกลับ/สลับลำดับ)
- `-h` : Human (คนอ่านออก)
