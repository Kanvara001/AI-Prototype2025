# 💻 Lecture Class I: Terminal Essentials
> สรุปคำสั่ง Command Line พื้นฐานและการจัดการระบบ

---

### 📍 1. การเช็กตำแหน่งปัจจุบัน
<details>
<summary>แสดง Path ที่เรากำลังทำงานอยู่</summary>

```bash
pwd
</details>

📂 2. การเรียกดูรายชื่อไฟล์ (List)
<details> <summary>แสดงไฟล์และโฟลเดอร์ในตำแหน่งปัจจุบัน</summary>

Bash

ls          # แสดงรายชื่อทั่วไป
ls -l       # แสดงรายละเอียดไฟล์แบบยาว (Permissions/Size/Date)
ls -lh      # แสดงขนาดไฟล์แบบอ่านง่าย (Human-readable)
ls -ltr     # เรียงตามเวลาแก้ไขล่าสุด (ไฟล์ใหม่ที่สุดอยู่ล่างสุด)
</details>

📁 3. การนำทางและเปลี่ยนโฟลเดอร์ (Navigation)
<details> <summary>คำสั่งสำหรับย้ายที่อยู่ไปยังโฟลเดอร์ต่างๆ</summary>

Bash

cd          # กลับไปยัง Home directory (~)
cd ..       # ย้อนกลับไป 1 ระดับ
cd ../..    # ย้อนกลับไป 2 ระดับ
cd <name>/  # เข้าไปยังโฟลเดอร์ที่ระบุ (กด Tab เพื่อช่วยเติมชื่อ)
</details>

🗂️ 4. การจัดการไฟล์และโฟลเดอร์
<details> <summary>การสร้าง แก้ไข และลบข้อมูล</summary>

สร้างโฟลเดอร์:

Bash

mkdir <folder_name>
สร้างและแก้ไขไฟล์ (VI Editor):

Bash

vi <file_name>
💡 Tip: กด i เพื่อเขียน | กด Esc แล้วพิมพ์ :wq เพื่อบันทึก | พิมพ์ :q! เพื่อออกโดยไม่บันทึก

การลบและดูเนื้อหา:

Bash

rm <file_name>      # ลบไฟล์
rm -r <folder_name> # ลบโฟลเดอร์และไฟล์ภายในทั้งหมด
cat <file_name>     # เปิดดูเนื้อหาในไฟล์
</details>

🚚 5. การเคลื่อนย้ายและคัดลอก
<details> <summary>Move & Copy ไฟล์หรือโฟลเดอร์</summary>

การย้าย (Move):

Bash

mv <source> <destination>/.
mv ../<file_name> .         # ดึงไฟล์จากโฟลเดอร์ก่อนหน้ามาที่ปัจจุบัน
การคัดลอก (Copy):

Bash

cp <source> <destination>/.
cp -r <folder_source> .     # คัดลอกโฟลเดอร์มาที่ปัจจุบัน
</details>

🛠️ 6. ระบบและคู่มือการใช้งาน
<details> <summary>การเช็กระบบและดู Manual</summary>

Bash

man <command>  # ดูคู่มือคำสั่งนั้นๆ
htop           # ตรวจสอบการใช้งาน RAM และ CPU (Real-time)
</details>

🔐 สรุปเครื่องหมาย Permission
<details> <summary>ความหมายของสัญลักษณ์ drwxr-xr-x</summary>

d → directory (โฟลเดอร์)

r → read (อ่าน)

w → write (เขียน/แก้ไข)

x → execute (รันโปรแกรม)

</details>
