# 💻 Command Line Basics (Terminal)
📘 Lecture Class I

เอกสารนี้สรุปคำสั่งพื้นฐานของ Command Line ที่ใช้บ่อยในการทำงานกับไฟล์ โฟลเดอร์ และระบบ ผ่าน Terminal  
เหมาะสำหรับผู้เริ่มต้นและใช้เป็น Reference ในการเรียน

---

## 📂 1. File Listing – การแสดงรายการไฟล์
ใช้คำสั่ง `ls` เพื่อดูไฟล์และโฟลเดอร์ใน directory ปัจจุบัน

### คำสั่งที่ใช้บ่อย
```bash
ls
แสดงรายชื่อไฟล์และโฟลเดอร์ทั้งหมด

ls -l


แสดงรายละเอียดไฟล์แบบ Long Format
ประกอบด้วย Permission, จำนวน user, ขนาดไฟล์, วันที่ และชื่อไฟล์

ls -lh


แสดงขนาดไฟล์ในรูปแบบอ่านง่าย (Human-readable เช่น KB, MB)

ls -ltr


เรียงไฟล์ตามเวลาที่มีการแก้ไข

ไฟล์ล่าสุดจะแสดงอยู่ด้านล่าง

-t = sort by time

-r = reverse order

Permission เบื้องต้น

r → read (อ่าน)

w → write (เขียน)

x → execute (รันไฟล์)

📁 2. Change Directory – การเปลี่ยนตำแหน่งโฟลเดอร์

ใช้คำสั่ง cd เพื่อย้ายตำแหน่งการทำงาน

cd


กลับไปยัง Home directory (~)

cd ..


ย้อนกลับไปโฟลเดอร์ก่อนหน้า 1 ระดับ

cd ../..


ย้อนกลับ 2 ระดับ

cd <folder_name>/


เข้าสู่โฟลเดอร์ที่ต้องการ
💡 สามารถกด Tab เพื่อช่วยเติมชื่อโฟลเดอร์อัตโนมัติ

pwd


แสดงตำแหน่ง path ปัจจุบัน

🗂️ 3. File & Folder Management
สร้างโฟลเดอร์
mkdir <folder_name>

สร้างหรือแก้ไขไฟล์ด้วย vi
vi <file_name>
vi <file_name>.py

การใช้งาน vi เบื้องต้น

i → เข้าสู่โหมดพิมพ์

Esc → ออกจากโหมดพิมพ์

:wq → บันทึกและออก

:q! → ออกโดยไม่บันทึก

ลบไฟล์
rm <file_name>

ลบโฟลเดอร์ทั้งหมด
rm -r <folder_name>

เปิดดูเนื้อหาไฟล์
cat <file_name>

🚚 4. Move File – การย้ายไฟล์
วิธีที่ 1 : ระบุ path ต้นทางและปลายทาง
mv <source_path>/<file_name> <destination_path>/.

วิธีที่ 2 : อยู่ในโฟลเดอร์ปลายทาง
mv ../<file_name> .


. หมายถึง directory ปัจจุบัน

📋 5. Copy File / Folder
Copy ไฟล์
cp <source_path>/<file_name> <destination_path>/.

Copy โฟลเดอร์
cp -r <folder_source> .

📖 6. Manual Page

ใช้ดูคู่มือและรายละเอียดของคำสั่ง

man <command>

🖥️ 7. System Monitoring
htop


ใช้ตรวจสอบการใช้งาน CPU และ RAM แบบ real-time
⚠️ ต้องติดตั้งก่อนใช้งาน

📝 Notes เพิ่มเติม

ตัวอย่าง permission:

drwxr-xr-x


ความหมาย:

d → directory

r → read

w → write

x → execute

คำสั่งที่ควรรู้:

ls -lh → แสดงขนาดไฟล์แบบอ่านง่าย

ls -ltr → ไฟล์ล่าสุดอยู่ด้านล่าง
