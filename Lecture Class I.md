# 💻 Command Line Essentials
> **📘 Lecture Class 1** | สรุปคำสั่งพื้นฐานและการใช้งาน Terminal สำหรับผู้เริ่มต้น

---

## 📂 1. การแสดงรายการไฟล์ (Listing)
ใช้ตรวจสอบข้อมูลภายในโฟลเดอร์ปัจจุบัน

| คำสั่ง | รูปแบบการแสดงผล / ความหมาย |
| :--- | :--- |
| `ls` | แสดงรายชื่อไฟล์และโฟลเดอร์ทั่วไป |
| `ls -l` | **Long Format**: แสดงสิทธิ์ (Permission), จำนวน User, ขนาดไฟล์, วันเวลา |
| `ls -lh` | **Human Readable**: แสดงขนาดไฟล์เป็นหน่วยที่อ่านง่าย (KB, MB, GB) |
| `ls -ltr` | **Time Reverse**: เรียงไฟล์เก่าไปใหม่ (ไฟล์ล่าสุดจะอยู่ล่างสุดเสมอ) |

---

## 📁 2. การนำทางและตำแหน่ง (Navigation)
จัดการตำแหน่งโฟลเดอร์ที่กำลังทำงานอยู่

| คำสั่ง | การทำงาน |
| :--- | :--- |
| `pwd` | **Print Working Directory**: ตรวจสอบว่าตอนนี้เราอยู่ที่ Path ไหน |
| `cd` | กลับไปยัง **Home Directory** (`~`) |
| `cd ..` | ย้อนกลับไปโฟลเดอร์ชั้นก่อนหน้า (1 ระดับ) |
| `cd ../..` | ย้อนกลับไปโฟลเดอร์ 2 ระดับ |
| `cd <name>` | เข้าไปยังโฟลเดอร์ที่ระบุ (ใช้ `Tab` เพื่อ Auto-complete ชื่อได้) |

---

## 🗂️ 3. การจัดการไฟล์และโฟลเดอร์ (File Management)
ครอบคลุมการสร้าง แก้ไข และลบข้อมูล

| งานที่ต้องการ | คำสั่ง | หมายเหตุ |
| :--- | :--- | :--- |
| **สร้างโฟลเดอร์** | `mkdir <name>` | สร้าง Directory ใหม่ในตำแหน่งปัจจุบัน |
| **สร้าง/แก้ไขไฟล์** | `vi <file_name>` | เปิดเครื่องมือแก้ไขข้อความ (VI Editor) |
| **ดูเนื้อหาไฟล์** | `cat <file_name>` | แสดงข้อความภายในไฟล์ออกมาที่ Terminal |
| **ลบไฟล์** | `rm <file_name>` | ลบไฟล์ถาวร (เรียกคืนไม่ได้) |
| **ลบทั้งโฟลเดอร์** | `rm -r <folder>` | ลบโฟลเดอร์พร้อมไฟล์ข้างในทั้งหมด |

> [!TIP]
> **การใช้ VI Editor:** กด `i` เพื่อพิมพ์ | กด `Esc` แล้วพิมพ์ `:wq` เพื่อบันทึกและออก | พิมพ์ `:q!` เพื่อออกโดยไม่บันทึก

---

## 🚚 4. การย้ายและคัดลอก (Move & Copy)
จัดการตำแหน่งของไฟล์และโฟลเดอร์

| ประเภทการทำงาน | รูปแบบคำสั่ง |
| :--- | :--- |
| **ย้ายไฟล์ (Move)** | `mv <ต้นทาง> <ปลายทาง>/.` |
| **เปลี่ยนชื่อไฟล์** | `mv <ชื่อเก่า> <ชื่อใหม่>` |
| **คัดลอกไฟล์ (Copy)** | `cp <ต้นทาง> <ปลายทาง>/.` |
| **คัดลอกโฟลเดอร์** | `cp -r <ต้นทาง> <ปลายทาง>` |

---

## 🛠️ 5. ตรวจสอบระบบและคู่มือ (Utility)
เครื่องมือช่วยเหลือสำหรับ Developer

| คำสั่ง | ฟังก์ชันการทำงาน |
| :--- | :--- |
| `man <command>` | **Manual**: เปิดดูคู่มือการใช้งานของคำสั่งนั้นๆ |
| `htop` | **System Monitor**: ดูการใช้ CPU/RAM (ต้องติดตั้งเพิ่ม) |
| `history` | แสดงรายการคำสั่งทั้งหมดที่เคยพิมพ์ไปก่อนหน้า |

---

## 🔐 6. โครงสร้างสิทธิ์การใช้งาน (Permissions)
ตารางสรุปความหมายของสัญลักษณ์เมื่อใช้คำสั่ง `ls -l`

| สัญลักษณ์ | ความหมาย |
| :--- | :--- |
| `d` | **Directory**: ตำแหน่งนี้คือโฟลเดอร์ |
| `r` | **Read**: สิทธิ์ในการอ่านไฟล์ |
| `w` | **Write**: สิทธิ์ในการแก้ไขหรือเขียนไฟล์ |
| `x` | **Execute**: สิทธิ์ในการรันโปรแกรม/สคริปต์ |
